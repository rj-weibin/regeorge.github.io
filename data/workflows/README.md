# data/workflows：工作流事实

这里保存经确认的工作流实体（自动化流程、Agent 协作协议、可复用的执行步骤）。

当前为空。当某个自动化或 Agent 流程被确认、需要作为事实源暴露给 MCP / Automation / 页面时，在此写入结构化实体（含 `id`、`type`、`status`、`updatedAt`、`source`）。

不要只因为“自动化里有一段脚本”就写入——工作流实体是**被确认后沉淀的可复用流程**，不是配置快照。
