# data/identity：身份与偏好

这里保存经确认的个人身份、长期偏好和稳定事实（如「原话优先」「分类边界」的落点）。

当前为空。仅写入**长期稳定、非临时**的身份事实；短期状态、情绪或具体某天的经历归属 `data/memory/`。

每个实体必须有 `id`、`type`、`status`、`updatedAt`、`source`，状态为 `draft` / `proposed` / `reviewed` / `archived`。
