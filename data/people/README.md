# data/people：人物

这里保存经确认的人物实体（关系、角色、长期交集）。

当前为空。写入标准：一个人物与本知识库有**持续**关联（出现在多篇 memory、多个项目中），且关系已确认。一次性的闲聊对象、泛指的“朋友”“教练”不构成人物实体。

字段建议：`id`、`type: person`、`status`、`updatedAt`、`source`、`links`（指向相关实体）。
